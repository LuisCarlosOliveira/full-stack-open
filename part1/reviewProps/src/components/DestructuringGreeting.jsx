const DesGreeting = ({name, age}) => {
    return (
      <div>
        <h1>Destructuring Greeting</h1>
        <h1>Hello, {name}!</h1>
        <p>You are {age} years old.</p>
      </div>
    );
  };
  
  export default DesGreeting;